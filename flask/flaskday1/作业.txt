1. 通过命令行启动Flask脚本，并传递启动参数，要求对URL中传递的name、score参数进行捕获，
   并使用相关的转换器进行参数转换，最后使用make_response()函数生成的响应对象对
   接收的参数返回。


2.使用捕获参数的path转换器，对路由中捕获的参数进行转换，观察
  "/vip/<name>/<path:info>/<home>/"中info参数的结果


3.首先通过URL传递一个参数,最后在视图函数中重定向到
   "http://httpbin.org/get",并在该路径中传递刚才URL捕获到的参数，观察结果。
  提示：使用"http://httpbin.org/get?参数名=参数值"的方式传递参数。
