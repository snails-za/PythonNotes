作业：
   1. 使用Flask-Cache缓存插件对学生信息进行缓存，要求在缓存时间内，
      如果缓存中存在同一个学号的学生，则直接返回，否则从数据库查询
      该学号的学生，以供下次从缓存中获取。

   2.使用请求钩子函数完成，只有用户登录后才能通过"/success/"
      访问成功页面，如果没有登录，则在请求钩子函数中返回响应
      “您还未登录，请先登录”.(使用request.path获取当前请求访问的路由)

   3. 使用Flask-SQLAlchemy完成“成员(Member)”-“社团(Community)”关系模型
      的创建，并进行迁移，添加相关数据，并分别从成员查询社团；从社团查询成员。